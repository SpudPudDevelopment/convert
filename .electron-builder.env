# Electron Builder Environment Variables
# These help prevent build hangs and improve performance

# Disable code signing for faster builds (remove if you need signing)
CSC_IDENTITY_AUTO_DISCOVERY=false

# Set Node.js options for better performance
NODE_OPTIONS=--max-old-space-size=4096

# Disable telemetry
ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES=true

# Set timeout for operations
ELECTRON_BUILDER_TIMEOUT=300000

# Enable verbose logging for debugging
DEBUG=electron-builder
